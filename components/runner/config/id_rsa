# Placeholder — replace this file with your actual SSH private key before applying
# (or remove the snapcd-runner-ssh-key entry from components/runner/kustomization.yaml
# if you don't need SSH cloning).
#
# Do not commit your real key. Either keep it untracked locally
# (`git update-index --skip-worktree components/runner/config/id_rsa`)
# or maintain it in a private fork of this repo.
